import { createTheme } from "@mui/material/styles";

const colors = {
  skyBlue: "#6386C7"
};

export const libraryTheme = createTheme({
  palette: {
    primary: {
      main: colors.skyBlue
    }
  }
});
